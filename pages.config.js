// Cloudflare Pages configuration
export default {
  // Disable Node.js compatibility features to avoid Symbol.iterator error
  nodejsCompat: false,
  // Build output directory
  buildOutputDirectory: ".output/public"
};
